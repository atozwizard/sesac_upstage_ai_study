"""
02.2주차_알고리즘 - Day 2: 배열과탐색
"""

from dataclasses import dataclass
from typing import List, Dict

@dataclass
class LearningContent:
    title: str = ""
    description: str = ""
    concepts: List[str] = None
    examples: List[str] = None
    key_takeaways: List[str] = None

    def __post_init__(self):
        if self.concepts is None:
            self.concepts = []
        if self.examples is None:
            self.examples = []
        if self.key_takeaways is None:
            self.key_takeaways = []

class Day2Learning:
    """Day 2: 배열과 탐색 알고리즘"""

    array_basics = LearningContent(
        title="2.1 배열(Array)의 기초와 특성",
        description="""
        배열은 같은 타입의 요소들을 연속된 메모리 위치에 저장하는 선형 자료구조입니다. 
        인덱스를 통해 O(1) 시간에 요소에 접근할 수 있지만, 요소 삽입/삭제는 O(n) 시간이 소요됩니다.
        파이썬의 리스트는 동적 배열로 구현되어 자동으로 크기를 조절하지만, 
        C나 Java의 배열은 고정 크기입니다. 배열의 특성을 이해하는 것은 
        문제 해결 및 자료구조 선택의 첫 단계입니다.
        """,
        concepts=[
            "배열의 메모리 구조와 인덱싱",
            "무작위 접근(Random Access) O(1)",
            "삽입/삭제의 비효율성 O(n)",
            "배열 크기와 용량의 개념",
            "동적 배열의 재할당",
            "2D 배열과 행 우선/열 우선 저장"
        ],
        examples=[
            "1차원 배열 생성과 접근: arr = [1,2,3,4,5]",
            "배열 끝에 추가: append() O(1 amortized)",
            "배열 중간에 삽입: insert(2, 99) O(n)",
            "배열 제거: pop(), remove() O(n)",
            "2D 배열: matrix = [[1,2],[3,4]]"
        ],
        key_takeaways=[
            "배열은 빠른 접근이 필요한 경우 최적",
            "빈번한 삽입/삭제가 필요하면 연결 리스트 고려",
            "동적 배열은 메모리 재할당 오버헤드 존재"
        ]
    )

    linear_search = LearningContent(
        title="2.2 선형 탐색과 Two Sum 문제",
        description="""
        선형 탐색은 배열의 첫 번째부터 끝까지 순서대로 원하는 요소를 찾는 방법입니다.
        시간복잡도는 O(n)이며, 정렬되지 않은 배열에서는 이것이 유일한 방법입니다.
        Two Sum 문제는 주어진 배열에서 두 수의 합이 목표값이 되는 인덱스를 찾는 문제입니다.
        순진한 접근은 O(n²)이지만, 해시맵을 사용하면 O(n) 시간에 해결할 수 있습니다.
        이는 시간-공간 트레이드오프의 좋은 예시입니다.
        """,
        concepts=[
            "선형 탐색 알고리즘",
            "Two Sum 문제의 정의",
            "해시맵을 활용한 최적화",
            "중복 처리와 인덱스 관리",
            "여러 케이스 처리 (정렬, 정렬 안 함)",
            "확장: Three Sum, k Sum 문제"
        ],
        examples=[
            "선형 탐색: nums = [5,3,8,1], target = 8 → 인덱스 2",
            "Two Sum (brute force): O(n²) - 모든 쌍 확인",
            "Two Sum (해시맵): O(n) - 한 번의 순회",
            "중복된 값 처리: target = 10, nums = [5,5,3]",
            "정렬된 배열의 Two Sum: 투 포인터 기법으로 O(n)"
        ],
        key_takeaways=[
            "정렬되지 않은 배열에서는 선형 탐색이 기본",
            "해시맵을 활용하면 탐색 속도를 O(n²)에서 O(n)으로 개선 가능",
            "문제의 특성에 맞는 자료구조 선택이 중요"
        ]
    )

    binary_search = LearningContent(
        title="2.3 이진 탐색(Binary Search)",
        description="""
        이진 탐색은 정렬된 배열에서 분할 정복을 이용하여 O(log n) 시간에 원하는 요소를 찾습니다.
        배열의 중간값과 찾는 값을 비교하여 범위를 반으로 줄여나갑니다.
        이진 탐색은 매우 효율적이지만 반드시 정렬된 배열이어야 합니다.
        반복문 또는 재귀로 구현할 수 있으며, 
        경계 조건 처리가 까다로운 부분입니다.
        """,
        concepts=[
            "이진 탐색의 기본 원리",
            "정렬 배열의 필수성",
            "분할 정복 전략",
            "반복 vs 재귀 구현",
            "경계 조건 (왼쪽, 오른쪽 포인터)",
            "lower_bound, upper_bound 개념"
        ],
        examples=[
            "기본 이진 탐색: arr = [1,3,5,7,9], target = 5 → 인덱스 2",
            "element not found: target = 6 → -1",
            "정렬되지 않은 배열: 이진 탐색 불가능",
            "lower_bound 구하기: 첫 번째 ≥ target 위치",
            "upper_bound 구하기: 첫 번째 > target 위치"
        ],
        key_takeaways=[
            "정렬된 배열에서는 선형 탐색 대신 이진 탐색 사용",
            "O(log n) 성능은 큰 배열에서 매우 우수",
            "경계 조건 처리에 주의 필요"
        ]
    )

    sliding_window = LearningContent(
        title="2.4 슬라이딩 윈도우 기법",
        description="""
        슬라이딩 윈도우는 배열의 연속된 부분배열(subarray)에 대해 효율적으로 계산하는 기법입니다.
        일반적으로 두 개의 포인터(left, right)를 사용하여 윈도우 크기를 조절합니다.
        중복 계산을 피하고 O(n) 시간에 여러 케이스를 처리할 수 있습니다.
        문자열 문제, 배열 부분합 문제 등에서 효과적으로 사용됩니다.
        """,
        concepts=[
            "슬라이딩 윈도우의 개념",
            "고정 크기 윈도우",
            "가변 크기 윈도우",
            "두 포인터 기법",
            "윈도우 상태 관리",
            "최적화 조건"
        ],
        examples=[
            "최대 부분합 (크기 k): 합 = sum(arr[i:i+k])",
            "길이 3인 부분배열 최대합: [1,2,3,4,5] → [3,4,5]",
            "문자열 중복 없이 최장 부분문자열: 'abcabcbb' → 'abc'",
            "특정 합을 갖는 최소 부분배열: target = 7, arr = [2,3,1,2,4,3]",
            "평균이 최소인 부분배열 (크기 k)"
        ],
        key_takeaways=[
            "슬라이딩 윈도우로 O(n²)을 O(n)으로 개선 가능",
            "중복 계산을 제거하여 효율성 향상",
            "고정/가변 크기 윈도우를 문제에 맞게 선택"
        ]
    )

    array_optimization = LearningContent(
        title="2.5 배열 문제 풀이 및 최적화",
        description="""
        배열 관련 알고리즘 문제를 해결할 때는 먼저 문제를 명확히 이해하고 
        가장 간단한 풀이부터 시작하여 단계적으로 최적화합니다.
        배열의 특성(정렬 여부, 중복 가능성)과 제약 조건(시간/공간)을 파악하여
        적절한 알고리즘을 선택합니다. 
        면접이나 코테에서는 공간을 희생하고 시간을 절약하는 트레이드오프가 일반적입니다.
        """,
        concepts=[
            "배열 정렬 여부에 따른 알고리즘 선택",
            "여러 해법의 시간/공간 비교",
            "in-place 알고리즘",
            "조건부 재구성 (조건 만족 요소 앞으로)",
            "회전과 반전",
            "면접 대비 문제 풀이 전략"
        ],
        examples=[
            "Remove Duplicates: O(n) 시간, O(1) 공간 in-place",
            "Move Zeros: 0이 아닌 요소를 앞으로",
            "Rotate Array: 배열 회전 (in-place)",
            "Container With Most Water: 높이와 너비로 최대 넓이",
            "Product of Array Except Self: O(n) 시간, O(n) 공간"
        ],
        key_takeaways=[
            "배열 문제는 먼저 정렬 여부를 확인",
            "해시맵/세트로 중복 처리 효율화",
            "in-place 솔루션으로 공간 최적화"
        ]
    )

    @classmethod
    def get_all_content(cls) -> Dict[str, LearningContent]:
        """모든 학습 내용을 딕셔너리로 반환"""
        return {
            "array_basics": cls.array_basics,
            "linear_search": cls.linear_search,
            "binary_search": cls.binary_search,
            "sliding_window": cls.sliding_window,
            "array_optimization": cls.array_optimization,
        }

    @classmethod
    def print_summary(cls) -> None:
        """학습 내용 요약 출력"""
        contents = cls.get_all_content()
        print("=" * 70)
        print("📚 Day 2: 배열과 탐색 알고리즘")
        print("=" * 70)
        for key, content in contents.items():
            print(f"\n📌 {content.title}")
            print(f"설명: {content.description[:100]}...")
